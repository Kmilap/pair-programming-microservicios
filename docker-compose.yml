# ============================================================
# docker-compose.yml — Pair Programming Microservicios
# Fase 2 — Arquitectura de Software UNAB
# ============================================================

services:
  # ----------------------------------------------------------
  # DOCKER SOCKET PROXY — Acceso seguro al daemon
  # ----------------------------------------------------------
  docker-socket-proxy:
    image: tecnativa/docker-socket-proxy:latest
    container_name: pp_docker_socket_proxy
    restart: unless-stopped
    environment:
      CONTAINERS: 1
      NETWORKS: 1
      SERVICES: 1
      TASKS: 1
      INFO: 1
      VERSION: 1
      POST: 0
      BUILD: 0
      COMMIT: 0
      EXEC: 0
      VOLUMES: 0
      IMAGES: 0
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    networks:
      - app-network
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--spider", "http://localhost:2375/_ping"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  # ----------------------------------------------------------
  # API GATEWAY — Traefik
  # ----------------------------------------------------------
  traefik:
    image: traefik:v2.11
    container_name: pp_traefik
    restart: unless-stopped
    command:
      - "--providers.docker=true"
      - "--providers.docker.endpoint=tcp://docker-socket-proxy:2375"
      - "--providers.docker.exposedbydefault=false"
      - "--providers.docker.network=pp_app_network"
      - "--entrypoints.web.address=:80"
      - "--api.dashboard=true"
      - "--api.insecure=true"
      - "--log.level=INFO"
      - "--accesslog=true"
    ports:
      - "80:80"
      - "8080:8080"
    networks:
      - app-network
    depends_on:
      docker-socket-proxy:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--spider", "http://localhost:8080/api/rawdata"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  # ----------------------------------------------------------
  # DATABASE — PostgreSQL para Auth
  # ----------------------------------------------------------
  postgres-auth:
    image: postgres:16-alpine
    container_name: pp_postgres_auth
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_AUTH_DB}
      POSTGRES_USER: ${POSTGRES_AUTH_USER}
      POSTGRES_PASSWORD: ${POSTGRES_AUTH_PASSWORD}
    volumes:
      - postgres_auth_data:/var/lib/postgresql/data
    networks:
      - app-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_AUTH_USER} -d ${POSTGRES_AUTH_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  # ----------------------------------------------------------
  # MICROSERVICIO — Auth (Laravel 12 + JWT)
  # ----------------------------------------------------------
  auth:
    build:
      context: ./services/auth
      dockerfile: Dockerfile
    container_name: pp_auth
    restart: unless-stopped
    environment:
      APP_ENV: local
      APP_DEBUG: "true"
      APP_KEY: ${AUTH_APP_KEY}
      DB_CONNECTION: pgsql
      DB_HOST: postgres-auth
      DB_PORT: 5432
      DB_DATABASE: ${POSTGRES_AUTH_DB}
      DB_USERNAME: ${POSTGRES_AUTH_USER}
      DB_PASSWORD: ${POSTGRES_AUTH_PASSWORD}
      JWT_SECRET: ${JWT_SECRET}
      JWT_TTL: ${JWT_TTL}
      JWT_BLACKLIST_ENABLED: "false"
      CACHE_STORE: array
      LOG_CHANNEL: stderr
    depends_on:
      postgres-auth:
        condition: service_healthy
    networks:
      - app-network
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.auth.rule=PathPrefix(`/auth`)"
      - "traefik.http.routers.auth.entrypoints=web"
      - "traefik.http.services.auth.loadbalancer.server.port=8000"
      # MIDDLEWARE CORS
      - "traefik.http.middlewares.cors.headers.accesscontrolalloworiginlist=http://localhost:5173"
      - "traefik.http.middlewares.cors.headers.accesscontrolallowmethods=GET,POST,PUT,DELETE,OPTIONS"
      - "traefik.http.middlewares.cors.headers.accesscontrolallowheaders=Content-Type,Authorization"
      - "traefik.http.middlewares.cors.headers.accesscontrolmaxage=100"
      - "traefik.http.middlewares.cors.headers.addvaryheader=true"
      - "traefik.http.routers.auth.middlewares=cors"
    healthcheck:
      test: ["CMD-SHELL", "php -r \"exit(@file_get_contents('http://localhost:8000/auth/health') ? 0 : 1);\""]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 60s

  # ----------------------------------------------------------
  # DATABASE — PostgreSQL para Pair Programming
  # ----------------------------------------------------------
  postgres-pp:
    image: postgres:16-alpine
    container_name: pp_postgres_pp
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_PP_DB}
      POSTGRES_USER: ${POSTGRES_PP_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PP_PASSWORD}
    volumes:
      - postgres_pp_data:/var/lib/postgresql/data
    networks:
      - app-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_PP_USER} -d ${POSTGRES_PP_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  # ----------------------------------------------------------
  # DATABASE — PostgreSQL para AI Engine
  # ----------------------------------------------------------
  postgres-ai:
    image: postgres:16-alpine
    container_name: pp_postgres_ai
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_AI_DB}
      POSTGRES_USER: ${POSTGRES_AI_USER}
      POSTGRES_PASSWORD: ${POSTGRES_AI_PASSWORD}
    volumes:
      - postgres_ai_data:/var/lib/postgresql/data
    networks:
      - app-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_AI_USER} -d ${POSTGRES_AI_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  # ----------------------------------------------------------
  # MESSAGE BROKER — RabbitMQ
  # ----------------------------------------------------------
  rabbitmq:
    image: rabbitmq:3.13-management-alpine
    container_name: pp_rabbitmq
    restart: unless-stopped
    environment:
      RABBITMQ_DEFAULT_USER: ${RABBITMQ_USER}
      RABBITMQ_DEFAULT_PASS: ${RABBITMQ_PASSWORD}
    ports:
      - "5672:5672"
      - "15672:15672"
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq
    networks:
      - app-network
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "ping"]
      interval: 15s
      timeout: 10s
      retries: 5
      start_period: 30s

  # ----------------------------------------------------------
  # CACHE / STATE — Redis
  # ----------------------------------------------------------
  redis:
    image: redis:7-alpine
    container_name: pp_redis
    restart: unless-stopped
    command: redis-server --appendonly yes
    volumes:
      - redis_data:/data
    networks:
      - app-network
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 5s

  # ----------------------------------------------------------
  # MICROSERVICIO — Editor Colaborativo (Node + Express)
  # ----------------------------------------------------------
  # Día 3: stub que responde POST /editor/rooms con payload fake
  #        para que Pair Programming complete el patrón Aggregator.
  # Día 4: este mismo servicio se amplía con Yjs + WebSocket
  #        para sincronización colaborativa real (CRDT).
  # Patrones aplicados:
  #  - Health Check (/editor/health)
  #  - Database per Service (Redis dedicado, se usa desde Día 4)
  # ----------------------------------------------------------
  editor:
    build:
      context: ./services/editor
      dockerfile: Dockerfile
    container_name: pp_editor
    restart: unless-stopped
    networks:
      - app-network
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.editor.rule=PathPrefix(`/editor`)"
      - "traefik.http.routers.editor.entrypoints=web"
      - "traefik.http.services.editor.loadbalancer.server.port=3000"
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:3000/editor/health || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 20s
  # ----------------------------------------------------------
  # MICROSERVICIO — Pair Programming (Laravel 12 + PHP 8.3)
  # ----------------------------------------------------------
  # Responsabilidad: gestión de sesiones de pair programming.
  # Orquesta el inicio de sesión llamando a Auth y al Editor,
  # persiste estado en su DB aislada y publica eventos al broker.
  #
  # Patrones aplicados:
  #  - Database per Service (su propio PostgreSQL)
  #  - Single Sign-On con JWT (valida tokens emitidos por Auth)
  #  - Aggregator (SessionController@start orquesta 2 servicios + DB)
  #  - Transactional Outbox (eventos persisten en DB, worker publica)
  #  - Asynchronous Messaging (publica session.started/ended)
  #  - Health Check (/sessions/health)
  # ----------------------------------------------------------
  pair-programming:
    build:
      context: ./services/pair-programming
      dockerfile: Dockerfile
    container_name: pp_pair_programming
    restart: unless-stopped
    environment:
      APP_ENV: local
      APP_DEBUG: "true"
      APP_KEY: ${PP_APP_KEY}
      APP_URL: http://localhost
      LOG_CHANNEL: stderr
      LOG_LEVEL: debug
      DB_CONNECTION: pgsql
      DB_HOST: postgres-pp
      DB_PORT: 5432
      DB_DATABASE: ${POSTGRES_PP_DB}
      DB_USERNAME: ${POSTGRES_PP_USER}
      DB_PASSWORD: ${POSTGRES_PP_PASSWORD}
      JWT_SECRET: ${JWT_SECRET}
      JWT_TTL: ${JWT_TTL}
      JWT_BLACKLIST_ENABLED: "false"
      CACHE_STORE: array
      SESSION_DRIVER: array
      AUTH_SERVICE_URL: http://auth:8000
      EDITOR_SERVICE_URL: http://editor:3000
      RABBITMQ_HOST: rabbitmq
      RABBITMQ_PORT: 5672
      RABBITMQ_USER: ${RABBITMQ_USER}
      RABBITMQ_PASSWORD: ${RABBITMQ_PASSWORD}
    depends_on:
      postgres-pp:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    volumes:
      - ./services/pair-programming:/app
      - /app/vendor
    networks:
      - app-network
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.pair-programming.rule=PathPrefix(`/sessions`) || PathPrefix(`/exercises`)"
      - "traefik.http.routers.pair-programming.entrypoints=web"
      - "traefik.http.services.pair-programming.loadbalancer.server.port=8000"
    healthcheck:
      test: ["CMD-SHELL", "php -r \"exit(@file_get_contents('http://127.0.0.1:8000/sessions/health') ? 0 : 1);\""]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 60s
# ----------------------------------------------------------
  # WORKER — Outbox Publisher (Transactional Outbox)
  # ----------------------------------------------------------
  # Cierra el ciclo del patrón Transactional Outbox.
  # Reutiliza la MISMA imagen que pair-programming (mismo código,
  # mismo vendor, mismo config) pero corre un comando artisan
  # distinto en lugar del servidor HTTP.
  #
  # Diseño:
  #  - Loop infinito que procesa la outbox cada 5 segundos.
  #  - Si crashea, restart: unless-stopped lo vuelve a levantar.
  #  - No expone puertos (no es un servicio HTTP).
  #  - No tiene labels Traefik (no recibe tráfico externo).
  # ----------------------------------------------------------
  pair-outbox-worker:
    build:
      context: ./services/pair-programming
      dockerfile: Dockerfile
    container_name: pp_pair_outbox_worker
    restart: unless-stopped
    command: ["sh", "-c", "php artisan outbox:publish --loop"]
    environment:
      APP_ENV: local
      APP_DEBUG: "true"
      APP_KEY: ${PP_APP_KEY}
      LOG_CHANNEL: stderr
      LOG_LEVEL: info
      DB_CONNECTION: pgsql
      DB_HOST: postgres-pp
      DB_PORT: 5432
      DB_DATABASE: ${POSTGRES_PP_DB}
      DB_USERNAME: ${POSTGRES_PP_USER}
      DB_PASSWORD: ${POSTGRES_PP_PASSWORD}
      CACHE_STORE: array
      SESSION_DRIVER: array
      RABBITMQ_HOST: rabbitmq
      RABBITMQ_PORT: 5672
      RABBITMQ_USER: ${RABBITMQ_USER}
      RABBITMQ_PASSWORD: ${RABBITMQ_PASSWORD}
    depends_on:
      postgres-pp:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    volumes:
      - ./services/pair-programming:/app
      - /app/vendor
    networks:
      - app-network
# ============================================================
# PERSISTENCIA Y REDES
# ============================================================
volumes:
  postgres_auth_data:
    name: pp_postgres_auth_data
  postgres_pp_data:
    name: pp_postgres_pp_data
  postgres_ai_data:
    name: pp_postgres_ai_data
  rabbitmq_data:
    name: pp_rabbitmq_data
  redis_data:
    name: pp_redis_data

networks:
  app-network:
    name: pp_app_network
    driver: bridge